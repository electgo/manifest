apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pim-master-dev
  namespace: argocd
spec:
  project: product
  source:
    repoURL: https://github.com/electgo/pim-master
    targetRevision: develop
    path: deployments/helm
    helm:
      releaseName: pim-master
      valueFiles:
        - values.yaml
        - values-dev.yaml
      parameters:
        - name: resources.requests.cpu
          value: "25m"
        - name: resources.requests.memory
          value: "128Mi"
        - name: ingress.host
          value: "pim.dev.electgo.com"
        - name: ingress.tls.enabled
          value: "true"
        - name: ingress.tls.secretName
          value: "pim-dev-tls"
        - name: ingress.className
          value: "haproxy"
        - name: nodeSelector.workload
          value: "apps"
  destination:
    name: development
    namespace: pim-master
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  # Secrets (env.* vars) managed via pim-master-env Secret — not in git.
  # Image tag bumped by CI (bump-image-tag job). Manual sync on dev.
